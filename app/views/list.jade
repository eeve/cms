a.btn.btn-default(ui-sref="write") 写一篇文章
hr
h1 所有文章
.row(ng-controller="PostsCtrl")
   .col-sm-12.col-md-12(ng-repeat="post in posts")
     .media
      .media-left
        a
          img.media-object(ng-src="{{post.image}}", width="80")
      .media-body
        .media-heading
          h4 {{post.title}}
          | {{post.desc}}
